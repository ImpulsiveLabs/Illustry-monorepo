---
title: Як вручну налаштувати Illustry
description: ручне налаштування
---
import { Tabs, TabItem } from '@astrojs/starlight/components';

## передумови

Перш ніж установлювати Illustry Frontend, переконайтеся, що у вас є такі передумови:

- Вузол версії 18.17.0 або новішої
- MongoDB

## Illustry бекенд


#### Локально:
У корені проекту потрібен файл із назвою `.env`, який містить такі поля:

```
MONGO_URL=mongodb:/127.0.0.1:27017/illustry
MONGO_TEST_URL=mongodb:/127.0.0.1:27017/illustrytest
MONGO_USER=<your_personal_mongo_user>
MONGO_PASSWORD=<your_personal_mongo_password>
ILLUSTRY_PORT=7000 
```
Виконайте такі команди:

<Tabs>
<TabItem label="npm">

```sh
npm i && npm run build:ts && npm run start:dev
```

</TabItem>
<TabItem label="pnpm">

```sh
pnpm install && pnpm run build:ts && pnpm run start:dev
```

</TabItem>
<TabItem label="Yarn">

```sh
yarn && yarn build:ts && yarn start:dev
```

</TabItem>
</Tabs>

#### Докер:

Виконайте такі команди:
```shell
docker build -t illustrybackend .
docker run -p 7000:7000 
```

## Illustry інтерфейс


#### Локально:
У корені проекту потрібен файл із назвою `.env`, який містить такі поля:

```
NEXT_PUBLIC_BACKEND_PUBLIC_URL: http:/0.0.0.0:7000
```
Виконайте такі команди:

<Tabs>
<TabItem label="npm">

```sh
npm i && npm run build:ts && npm run start:dev
```

</TabItem>
<TabItem label="pnpm">

```sh
pnpm install && pnpm run build:ts && pnpm run start:dev
```

</TabItem>
<TabItem label="Yarn">

```sh
yarn && yarn build:ts && yarn start:dev
```

</TabItem>
</Tabs>

#### Докер:
**Перед запуском створіть бекенд-образ!**

Виконайте такі команди:
```shell
docker build -t illustryfrontend .
docker run -p 3000:3000 -e NEXT_PUBLIC_BACKEND_PUBLIC_URL=http:/ilustrybackend:7000 illustryfrontend
```
